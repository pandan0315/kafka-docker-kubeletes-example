USER-SUPPLIED VALUES:
    cp-kafka:
      brokers: 1
      configurationOverrides:
        "offsets.topic.replication.factor": "1"
    cp-kafka-connect:
      enabled: false
    cp-kafka-rest:
      enabled: false
    cp-ksql-server:
      enabled: false
    cp-zookeeper:
      servers: 1